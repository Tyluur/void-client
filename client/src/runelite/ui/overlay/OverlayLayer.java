package runelite.ui.overlay;

/**
 * Layer in the rendering pipeline where an overlay should be rendered.
 */
public enum OverlayLayer
{
	ABOVE_SCENE,
	UNDER_WIDGETS,
	ABOVE_WIDGETS,
	ALWAYS_ON_TOP
}
